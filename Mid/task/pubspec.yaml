name: task
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
  flutter:
    sdk: flutter
  cached_network_image: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/7.png
    - assets/8.png
    - assets/9.png
